{
  "name": "inventory-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CineGearPro inventory app (Stocky replacement): full-lifecycle inventory ledger, adjustments with audit, virtual stock, stocktakes.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test 'test/*.test.js'"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.22.0"
  }
}
