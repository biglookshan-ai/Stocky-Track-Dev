# 进度 · Inventory

- **状态**: 开发中 · Lark 通知已可在 app 内自助配置，等待用户配群验收
- **进度**: 98%
- **一句话**: Lark 群地址和卡片内容都搬进 app 设置页，勾选式开关 + 实时预览 + 一键测试发送，不用再改服务器配置。
- **分类**: Shopify App

## 🔨 进行中
- Lark 通知验收：在 app「手动调整 → Lark 通知设置」粘贴群机器人 Webhook，点「发一条测试消息」核对群消息
- 订单生命周期验收：抽查 Purchased、Order Edited、Order fulfilled、取消/释放、退货重新入库与 Purchase order received
- 商品详情验收：抽查负库存、仅当前值、在途库存和多仓商品的趋势及状态说明
- P2 受控验收：更新 Shopify scopes 并重新授权，核对实时 webhook、三类人员、调整编号、附件及测试 SKU `+1 / -1` 回滚
- P1 持续验收：抽查全局搜索、修改记录筛选、Order 客户、Transfer 编号/状态和多状态数量
- 导航验收：在 Shopify 后台抽查独立地址、直接打开详情和顶部逐级返回

## ⏭ 下一步（近期计划，用户 2026-07-29 定）
- **测试补货规则功能**（验证补货建议/规则逻辑）
- **优化每一个现有界面**（逐页交互与排版打磨）
- P2.1 底层：销售历史数据模型与周/月/3月/半年/年度统计，形成补货建议基础
- P3：Stocky 历史导入 + 虚拟库存
- P4：Low stock / Lost revenue / Best sellers + 轻量盘点

## 🏁 最近完成
- **失败通知 + 撤销单区分（2026-08-10）**：①**提交失败现在会通知群里**(此前只有操作的人在页面看得到,关掉页面就没人知道)。红色卡片,区分两种失败:`rejected`(Shopify 明确拒绝→库存没变、单退回草稿、改完重提)与 `unknown`(请求无回音→结果未知、用同一幂等键安全重试),各给不同的处置说明;迁移 016 记住已播报的错误,同一错误重试不刷屏,成功后清除。②**撤销单单独标题+颜色**(默认橙色 `↩️ Adjustment undone`)并加 `**Undoes:** A0009` 行,群里一眼分得清。③设置页改为**三种卡片各自可配标题/颜色**,失败通知可单独关闭;预览区同时并排展示三种卡片(用同一张真实调整单演示,不必制造真失败)。新增 6 个单测
- **修复 502 全站宕机 + 加两道防线（2026-08-10）**：Lark 设置页报 `Cannot GET /lark-settings`——服务端 SPA 地址白名单漏加新页面（直接打开/刷新/收藏才会触发，点击进入不会）。改用共享 `src/spa-routes.js`，并加**双向**测试与 app.js 实际路由互校。修此问题时两处 import 因锚点写错静默未插入（用了无断言的字符串替换），`SPA_ROUTES` 在模块顶层引用 → 启动即崩，全站 502 约 3 分钟。补 `test/server-boot.test.js`：子进程加载 server.js，必须走到数据库检查才算通过（`node --check` 只做语法解析，抓不到「用了没导入的名字」）。两个测试都验证过能真的失败
- **Lark 通知自助设置（2026-08-10）**：迁移 015 建 `app_settings`(JSONB)；新页 `/lark-settings`(手动调整页入口)。①**群机器人 Webhook + 签名密钥搬进 app**，AES-256-GCM 加密入库(复用 token-store)，回传前台只给打码串，env 变量降为 fallback；②卡片**11 个内容开关**(原因/备注/明细/Barcode/SKU/仓位/前后数量/记录员工/经手员工/时间/详情按钮，明细的子项随父开关联动禁用)+标题模板(`{number}`)+6 种标题栏颜色；③**实时预览**——服务端用同一个 `buildAdjustmentNotificationMessages` 生成真实卡片 JSON，前端按 Lark 结构渲染(预览不可能与实发漂移)，优先用最近一张已提交调整单的真实商品；④**一键发测试消息**到群；⑤卡片文案全改英文(用户定「只用英文」)，`、`分隔改`, `。新增 8 个单测，旧 6 个断言同步改英文
- **中英文切换（2026-08-10）**：新增 `public/i18n.js` 语言层，`t('中文源串')` 查 EN 字典，**默认英文**，顶栏按钮切换，选择存 localStorage（每人各自记住，互不影响）。456 条界面文字全量覆盖（脚本校验 0 遗漏），另加 19 条正则规则翻译带变量的服务端报错（如 `第 3 行商品无效`）。**只翻界面固定文案**——备注/员工名/原因名/商品标题等手动输入内容一律原样输出（`esc(...)` 不经 `t()`）；日期按语言用 en-GB / zh-CN。新增 6 个 i18n 单测
- **一键撤销调整单（2026-08-10）**：迁移 014 加 `reversal_of_adjustment_id`。已提交（applied）的调整单详情页出现「撤销这张调整单」→ 生成数量相反的**草稿**（原因固定 `Reversal`、备注 `Reversal of A00xx-xxxxxx`、默认继承原单的记录/经手员工）→ 直接跳到编辑页，**可改数量、可删行**，确认后才写 Shopify。双向横幅互链（「这是一张撤销单」/「已被撤销」）；已有撤销单的不能再撤；原单含已从 Shopify 删除的商品时明确报错而非静默跳过
- **Lark 多行内容对齐修复（2026-08-04）**：不再用 Markdown 空格模拟缩进；备注改为固定标签栏 + 内容栏，产品明细改为编号栏 + 内容栏，备注续行、长产品标题自动换行、Barcode 与库存数字始终在各自内容栏内对齐，并新增卡片结构测试
- **Shopify App 正式更名（2026-08-04）**：正式 App 显示名保持 `Stock Track CGP`，Admin URL handle 从 `stocky-track-dev` 更新为 `stock-track-cgp`；项目默认值、Railway 环境变量示例和调整单/Lark 详情链接同步使用新地址
- **Lark 群调整通知（2026-08-04）**：提交按钮旁新增默认勾选的「通知」选项，可取消后只调整 Shopify；通知为绿色富文本卡片，原因和备注置顶，多行备注持续缩进对齐，明细优先显示红/绿 Change，再显示 Before/After；Barcode 可点击进入该变体的 Shopify 后台页，底部显示记录/经手员工、调整时间和详情按钮；超长整单自动拆分且记住已发部分，通知失败不回滚 Shopify 库存
- **新建调整单商品选择简化（2026-08-03）**：页面只保留「调整明细」和一个「添加商品」按钮；点击后在同一弹窗内输入 Barcode / SKU / 商品 / Brand、搜索、勾选变体并批量加入，加入结果直接回到调整明细；搜索接口返回真实总数并支持每页 50 个的上一页/下一页，跨页勾选保持；移除原页面重复搜索区
- **完整页面路由与返回导航（2026-08-03）**：旧 hash 半路由升级为独立路径（如 `/items/123`、`/history/456`、`/adjustments/789`），服务端支持直接打开/刷新详情；统一只保留顶栏返回按钮（移除不稳定的前进按钮及详情区重复返回），由 App 保存访问栈并逐级返回；直接打开详情时安全回默认列表；产品/修改记录/调整单/虚拟库存/已删除产品等筛选、排序和页码写入 URL 并可恢复；旧 `#/...` 链接自动迁移；新增导航单测并完成浏览器交互验收
- **虚拟库存管理（2026-08-03，08-04 简化）**：新页面 `/virtual-stock`（手动调整页入口）。口径极简：External Warehouse 不放实物，**该仓库当前有库存的商品 = 虚拟库存**，直接读 current_levels，不做账本推算。列出商品/数量/最近设置时间/最近备注（可点回调整单），顶部可切换仓库；勾选后一键生成撤销草稿把该仓库清零（仍需人工确认提交，不直接写 Shopify）。曾走过弯路：先按调整净额汇总（卖出走订单不走调整，净额只增不减，误报一堆已卖完/已删除产品），后按用户口径改为看仓库库存
- **员工可停用（2026-08-03）**：员工行加 Active 勾选（同调整原因）；离职员工取消勾选后不再出现在「记录员工/经手员工」下拉，历史记录照常显示。updateStaff 改为部分更新，避免简化表单清空其他字段
- **修复"点击没反应"的真因（2026-08-03）**：异步竞态——切换页面后旧页面的 fetch 回来仍写入已移除的元素，抛 `Cannot set properties of null` 被 route 捕获后整页变错误提示。加导航代次 navGeneration + 写入安全选择器 $w()（目标不存在时写入游离节点），36 处 innerHTML 写入全部改造
- **Shopify 账号 ≠ 员工**：账号邮箱无法自动获取（read_users 为 Plus 专属），改为账号行可手动改名为邮箱，只需设置一次
- **员工设置简化 + 性能加固（2026-08-03）**：员工资料只保留姓名(去掉员工编号/本地员工等术语)，Shopify 登录账号行标「登录账号」且不可删；保存/删除按钮统一宽度对齐；连接池 8→16 并加 statement_timeout(后台历史同步/导入的长事务曾把交互请求饿死)
- **UI/UX 大批改进（2026-08-03）**：①导航改为 首页/手动调整/库存历史/产品列表，去掉 CGP Inventory 品牌位；②首页顺序改为 最近3天库存变动→计数卡→数据同步；③产品列表「Shopify 已删除产品」改为右上按钮；④修改记录多变体事件可就地折叠展开(每商品一行+可点进商品)，不必再点进详情；⑤调整原因/员工新增删除功能(被历史引用的自动降级为停用并提示)，设置区排版溢出修复；⑥新建调整：搜索框移到调整明细下方、**搜索改 Shopify 式分词**(「dzofilm arles 100mm」不再搜不到)、结果支持勾选多选批量加入(变体级)、扫码精确命中仍即时加入
- **导入三修 + 已删除产品可读性（2026-08-02）**：①已删除/本地产品的历史修改记录此前被 `JOIN current_levels`(内连接)全过滤(计数36却空表)→改 LEFT JOIN，历史正常显示且每条链接回调整单；②本地商品名 CSV 有766行Product空→回退成"(Stocky)编号"，改为 产品名→变体名→SKU→编号，且同条码取最富样本(1861有名/377用变体/0只剩编号)；③已删除产品清单"出现在"去掉6个上限显示全部调整单；导入版本 bump 4
- **导入匹配加 SKU 兜底（2026-08-02 v3）**：修复真实 Shopify 产品被误判"本地"的 bug——①loadLookups 只查 plan.events 的条码(漏近期产品)且只按 barcode，现查 events+coveredEvents 全部 barcode+SKU；②新增 resolveItemId：barcode 匹配不上时用唯一非空 SKU 兜底(Stocky 导出条码常与 Shopify 不一致)；③所有解析点统一走 resolveItemId。导入版本 bump 到 3 自动重导。真实产品重新关联→其历史回到产品页、不再是本地副本
- **修复导入严重 bug + 布局重做（2026-08-02）**：①覆盖期(2026-01-29后)孤儿条码的本地商品此前被漏建(unmatchedBarcodes 只扫 plan.events 漏了 coveredEvents)，导致~150张单无明细行——改为扫 allEvents，192个条码补建；②导入用版本号(STOCKY_IMPORT_VERSION=2)驱动，部署自动 undo+重导自愈；③详情页重做：Notes 提到最前+醒目、原因/操作人上移、编号改显原 Stocky 号(#3267+Stocky导入标签)、加独立规格(Variant)列、null→—、Status 加说明tip；④撤销改真删无引用本地商品
- **导入可完全撤销/重做**：/import/stocky/undo（界面「撤销导入」按钮）——删除 STK 单+导入事件+明细，按 stocky_import_backfill 追踪表逐字段清回回填内容（只清导入填的空字段，绝不动 Shopify 原有数据/手动编辑），停用无引用的本地商品，清 one-shot 标记以便重导。幂等
- **旧员工/原因作为活数据源**：导入的 8 名员工 + 11 原因全部建为 active，成为新建调整的员工下拉+原因字典基础；原因方向按 -/+ 前缀自动判定；用户可在设置面板改名/改方向/停用
- **重叠期匹配回填**：2026-01-29 后的 203 张旧单不再跳过——逐行匹配现有正式记录（同商品+仓+delta+±36h+App=Stocky，交集唯一才认，歧义保守不动），匹配成功则把操作人/备注/原因回填进现有记录（COALESCE 只补空不覆盖）并把 STK 单挂到同一事件；产品历史不新增行
- **Stocky 历史导入=手动调整档案迁移**：每张旧单同时进「库存调整」工作区（display_number STK-xxxx 保留原编号，number 留空不占用新序列；原因/调整人(recorded_by+handled_by)/备注/合并明细行；历史原因建为 inactive 不进新建下拉）和修改记录时间线。维护工具支持上传 Stocky Adjustments CSV，预检（映射报告：导入/跳过/新建本地商品/员工/仓位/原因分布）→ 确认导入两步式；融合规则=只导入官方流水覆盖起点(2026-01-29)之前避免重复；导入事件标 Historical import、qty_after 显示未提供、不参与趋势重建（无当年销售数据不编造曲线）；幂等可重跑；未执行行(not_adjusted/failed)不入账；# 内部产品自动建为本地商品
- 占位合并三段式收口：精确匹配之外新增「delta 求和覆盖」（解决 webhook 把连续变动合并成一个累计 delta 后永远对不上的卡死问题，求和不符即保守不动）；48 小时仍无匹配的占位标记 stale 保留待查、不再计入「等待官方流水」
- **确定性记录改造（2026-07-31）**：所有用户可见的修改记录/商品历史/趋势/最近变动只显示 ShopifyQL 正式记录（每条必有操作人/App/原因/单据）；webhook 临时占位降级为纯内部数据，界面彻底移除「库存信息补全中」等不确定状态
- 修复自家调整单重复计数：ShopifyQL 回传同一调整组时按事件+状态+delta 认领本地已写入的行（保留员工/备注），不再插入重复审计行导致 delta 翻倍
- 移除归因 48 小时「external_app/n:a」兜底——不再产生任何无操作人的永久模糊标签
- 正式流水拉取改为 webhook 触发（60 秒防抖，5 分钟定时兜底），记录出现延迟从最多 5 分钟+ 压缩到 Shopify 报表管道的实际延迟
- 状态口径统一：/healthz 与系统状态页的等待计数改读「占位事件数」，与界面所见一致（原先明细行清零但界面仍显示补全中的矛盾已消除）
- 新增完整 `HANDOFF.md`，记录项目身份、全部已上线功能、真实库存数据原则、同步架构、数据库/API、部署、测试、开发时间线、故障排查、未完成功能与接手检查清单
- 移除用户界面的「库存有差异」复核流程；旧的 119 条内部告警由迁移统一归档，不修改 Shopify 库存
- 每日快照改为静默刷新 Shopify 当前库存与趋势检查点，不再追加 reconciliation 账本行、虚构操作时间或生成新告警
- 商品库存趋势排除旧版本 reconciliation 技术修正行，只展示 Shopify、员工或 App 返回的真实变动与当前值
- 占位合并从固定每 30 秒检查改为 Webhook 处理后事件触发；空闲时不再反复查询数据库
- 修复库存占位先被归因为 sale/refund 后失去合并资格的问题；正式 Shopify 记录出现后可自动清理重复占位，部署启动时也会立即清理一次
- 销售历史增加展示层防重复：同商品、仓位、数量且 30 秒内已有正式订单记录时，不再把 `Inventory updated` 占位重复计入下单和待出货
- 销售历史新增订单生命周期拆分：下单占用、真实出货、按订单推算的待出货、取消/释放和退货分别统计；下单但未履约的订单不再显示为“没有记录”
- 销售周期图与明细同步展示 Purchased / Order Edited 等需求事件；周均销量、预计可售天数和后续补货依据仍只使用真实净出货
- 商品详情新增销售历史面板：支持最近 7 天、30 天、3 个月、半年、一年和全部记录；展示销售出库、退货、净销售、采购入库、周均销量、预计可售天数、周期图表和业务明细
- 销量口径只采用 Order fulfilled 的 On hand 减少；退货与 Purchase order received 分开统计，排除下单占用、内部调拨、人工调整和 App 修正
- 商品库存趋势从“每日净变化”改为“逐次操作变化”，同日先减后加会显示完整波动；圆点悬浮补充 Activity、仓位和本次变化
- 所有前台 API、CSV 下载、附件预览与附件上传统一加入 session token 自动刷新；401 最多安全重试两次，并发请求共享同一次刷新，避免页面停留后出现 `session token expired`
- 新增过期令牌恢复、并发刷新去重和重试上限测试
- 商品详情库存区重构：四项总库存概览、各仓状态卡、Unavailable 组成说明，以及可切换 Available / On hand / Committed / Incoming、仓位和时间范围的动态阶梯趋势
- 趋势数据改由当前 Shopify 数量结合 append-only 修改记录反向重建；无历史时明确显示空状态，不再用稀疏快照绘制误导性的斜线
- 离线 Shopify token 改为使用 App Secret 加密后持久化到 Postgres；Railway 换容器后后台 Webhook、历史增量与每日核对可立即恢复
- ShopifyQL 正式记录晚于 Webhook 到达时，会按同商品、仓位、库存状态、数量和 30 秒时间窗自动合并重复占位事件；日常同步持续回看最近 2 天，避免「库存信息补全中」长期保留
- 修复历史补全重试时「已有正式记录 + 后到 Webhook 占位记录」触发唯一键冲突的问题，并覆盖 Shopify 额外库存状态的重复占位合并
- 部署恢复历史回填前强制幂等重扫最近 2 天，确保旧游标之前遗留的「库存信息补全中」也会被再次处理
- 日常 ShopifyQL 增量回看窗口扩大到最近 2 天，防止报表延迟数小时导致 Webhook 占位记录被永久跳过
- Railway 新旧容器重叠导致历史恢复锁忙时，后台会每 15 秒自动重试，不再留下“显示进行中但游标不动”的假运行状态
- 180 天 ShopifyQL 回填从逐日查询优化为最多 7 天一批、超量自动拆分，显著减少限流等待次数
- 手动回填和部署恢复会先补最近记录，再继续旧历史，避免「库存信息补全中」被长任务持续阻塞
- 历史同步进度补充当前回填日期与悬浮说明，明确百分比按批次跳动而非逐行增长
- 第一轮曾将技术状态统一为较易理解的名称；本版进一步移除本地推算差异与人工复核流程
- 差异说明明确本应用只校正本地记录、不修改 Shopify 库存；确认按钮改为更直观的「标记已确认」
- 实时接收注册会强制刷新离线 token、读取当前安装实际 scopes，并在页面精确列出待授权权限，不再只显示失败事件数量
- 补齐 Fulfillment Webhook 所需 `read_fulfillments`，并将 Order / Fulfillment / Transfer / Shipment topic 映射到各自权限
- 修复 Shopify 2026-04 不支持 `INVENTORY_TRANSFERS_UPDATED` 导致整批 Webhook 注册中断的问题，改用受支持的 Add/Remove items 事件
- Webhook 改为逐项注册和核心/扩展分级；单个调拨、运输事件缺少权限或版本不支持时，不再阻断库存核心实时接收
- webhook 库存变化先即时生成 Pending attribution 可见记录，ShopifyQL 后续原位补齐来源，避免最新记录长期停在旧日期
- 新调整单采用 `ADJ-YYYYMM-xxxxx` 稳定编号，区分 Shopify 登录账号、实际记录员工和多位经手员工
- 员工资料支持员工编号与不绑定 Shopify 账号的本地员工，人员可在调整详情和 CSV 中长期保留
- 顶栏全局搜索覆盖商品、Barcode、SKU、Brand、人员、调整编号、Order/Transfer；修改记录新增人员、来源和日期筛选
- Order 引用补充订单名称、客户、财务/履约状态和 Admin 链接；Transfer 补充准确编号、状态、起点/终点与链接
- 健康状态补充 webhook 最近接收/处理时间和错误数，实时性不再只用笼统文字表示
- 调整数量改为明确的 `− / +` 方向按钮和正整数数量，按 Before 自动计算 After；reason 可锁定允许方向
- Notes 升级为 10,000 字多行说明；Draft 支持图片、视频、PDF 与常用文件附件，每个 50 MB、每单 20 个
- 附件保存到 Railway `/data` Volume、元数据保存到 Postgres，仅认证员工可访问，危险脚本/可执行类型被拒绝
- 修改记录详情将缺失状态显示为「未提供」而非 `—`，说明其不等于 0，并展示 Shopify 返回的其他库存状态
- 新增「库存调整」工作区：列表筛选、状态、原因、员工、仓位、商品数、合计变化及详情
- 新建/编辑 Draft 支持 Barcode、SKU、标题和 Brand 搜索，多商品增减及 Before / Change / After 预览
- 提交 Shopify 前二次确认并重新读取当前 Available；使用 `changeFromQuantity` 阻止覆盖并发修改
- Shopify 2026-04 mutation 使用持久幂等键；网络状态未知可安全重试，成功后锁定为 Applied
- Adjustment reasons 支持方向和启停，员工 user ID 可映射显示名；调整单支持归档与筛选后 CSV 导出
- 本地即时记录只确认本次 Available 写入，其余库存状态等待 Shopify Webhook / ShopifyQL 真值回传，避免重复推算
- 新增调整输入、原因映射、并发 mutation input 与 CSV 转义单元测试
- 商品列表将 Barcode、SKU、Brand 合并到标题下方，腾出空间集中展示 Unavailable、Committed、Available、On hand、Incoming
- 首页与告警中的商品编号统一为无标签紧凑格式，仅 Barcode 加粗
- 修改记录的商品文字/变体数量整体可点击进入详情，仅保留箭头提示
- 移除标题副标题之间的负边距，统一页面标题、卡片标题和说明文字的垂直间距
- 首页最近 3 天修改商品固定每页 10 个并支持翻页，避免长列表挤占首页
- 全店修改记录的每次事件均可打开详情；多商品事件会展开到商品、Barcode、SKU、仓位和各库存状态变化
- 商品列表、商品详情、首页最近修改与告警统一以 Barcode 为第一识别字段，SKU 作为辅助信息
- 全站字号、表格行距、卡片留白和宽表布局统一收紧，减少拥挤换行
- 商品列表支持 Brand 和 Collection 筛选、Shopify Collection 默认顺序，以及 Available、品牌、名称、最近库存修改时间排序
- 商品列表补充当前 Available、最近修改时间和最近库存变化；详情页增加前台商品与 Shopify Admin 按钮
- 商品详情合并为单一「历史修改记录」，库存状态统一使用 Shopify 英文名称
- 首页各概览卡可跳转，新增最近 3 天修改商品；系统状态页可查看对账差异、打开 Shopify 调整并标记已复核
- 历史同步状态区分进行中、暂停和完成，并显示可解释的进度
- 信息架构重构：首页只展示商品、修改记录、历史范围和系统健康；技术队列与维护工具折叠并补充解释
- 商品修改记录支持全量分页和仓位筛选；Activity 使用 Shopify 风格英文名称
- 订单引用可直接打开 Shopify Admin；Created by 明确区分 Staff / App / System
- 历史回填支持部署中断后自动续跑，重任务锁改为进程退出自动释放的 Postgres advisory lock
- P1 商品级库存修改记录：ShopifyQL 事件同步、员工/App/原因/引用归因、Admin 风格多状态历史表
- P0 多状态准确性：Available / On hand / Committed / Incoming / Reserved / Damaged / Safety stock / Quality control 全量同步和逐项快照修复
- 商品页默认加载前 100 个，避免首次进入误显示“无结果”
- M0 三层账本骨架(2026-07):webhook HMAC 验证 + 原始幂等落库 + 后台处理、账本 append-only + current_levels 基线、归因匹配、每日快照 + 漂移自愈、商品目录同步(全量+增量)
- 迁移表结构全部建好(含 M2/M3 的调整/盘点/虚拟库存/BOM)
